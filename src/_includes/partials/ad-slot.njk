{# In-content ad slot — parked while the AdSense review is pending.
   Auto Ads (script in base.njk <head>) handles placement automatically once approved.
   To re-enable a manual unit after approval, restore the <ins class="adsbygoogle"> block here. #}
